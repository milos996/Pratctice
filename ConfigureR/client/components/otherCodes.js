// This is code that is used through training
/*
import React from 'react';
import {
  BrowserRouter as Router,
  Route,
  Link
} from 'react-router-dom'

export default class App extends React.Component {
  render() {
    return (
     <div >
          <nav>
            <Link to="/dashboard">Dashboard </Link>
          </nav>

          <div>
          <Route path="/dashboard" component={Dashboard} />
          </div>
      </div>
    );
  }
}

function Dashboard(props){
  console.log(props);
  return(
    <div>
  <h1>  Your now in Dashboard </h1>
    <div>
    <Link to={props.match.url + "/score"}>
    Score
    </Link>
    </div>

  <Route path={props.match.url + "/score"}
    component={Score}
    />
    </div>
  );
}

function Score(props){

  return(
    <h2>Score </h2>
  )
}




*/
